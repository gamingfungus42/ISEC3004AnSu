# ISEC 3004
# Instructions to get this b word running:
# Check install for: apache2, php, mysql 
# 
# To run the actual db:
# sudo mysql < schema.sql
# in (sudo) mysql> :
# CREATE USER 'test'@'localhost' IDENTIFIED BY 'password';
# GRANT ALL PRIVILEGES ON demodb.* TO 'test'@'localhost';
# FLUSH PRIVILEGES;
